{% extends 'baseadmin.html.twig' %}

{% block title 'Gestions de la page d accueil' %}

{% block body %}
<div class='container mt-4'>

<h2> Gérer les textes de l'accueil </h2>
<table class ="table table-striped">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for home in leshomes %}
        <tr>
            <td>{{ home.tittle }}</td>
            <td>
                <a href="{{ path('admin.home.edit',{id: home.id}) }}" class="btn btn-secondary">Editer</a>
                
                <form method="post" action="{{ path('admin.home.delete',{ id: home.id}) }}"  onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?')" style="display: inline-block">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home.id) }}"/>
                    <button class="btn btn-danger">Supprimer</button>
                </form>
            </td>

        </tr>
        {% endfor %}
    </tbody>
</table>
<div class ="text-right">
    <a href="{{path('admin.home.new')}}" class="btn btn-secondary">Ajouter un texte d'accueil</a>
</div>
</div>




{% endblock %}